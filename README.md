# 📋 TaskBot — Telegram-бот для управления задачами команды

Бот для управления задачами команды с системой напоминаний, интеграцией календаря и монетизацией через подписку.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка

Скопируйте файл настроек и укажите токен бота:

```bash
cp .env.example .env
```

Откройте `.env` и вставьте токен от [@BotFather](https://t.me/BotFather):

```env
BOT_TOKEN=ваш_токен_здесь
```

### 3. Запуск

```bash
python main.py
```

---

## 📝 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Регистрация и приветствие |
| `/help` | Справка по командам |
| `/menu` | Главное меню |
| `/createteam [имя]` | Создать команду |
| `/team` | Информация о команде |
| `/invite` | Получить инвайт-код |
| `/join [код]` | Присоединиться к команде |
| `/leave` | Покинуть команду |
| `/newtask` | Создать задачу (диалог) |
| `/mytasks` | Мои задачи |
| `/alltasks` | Все задачи команды |
| `/today` | Задачи на сегодня |
| `/week` | Задачи на неделю |
| `/task [ID]` | Детали задачи |
| `/stats` | Статистика команды |
| `/mystats` | Личная статистика |
| `/calendar` | Экспорт в .ics |
| `/subscribe` | Тарифные планы |
| `/billing` | Текущая подписка |
| `/settings` | Настройки |
| `/timezone [зона]` | Часовой пояс |
| `/cancel` | Отменить действие |

---

## 🏗 Архитектура проекта

```
taskbot/
├── main.py                      # Точка входа
├── config.py                    # Конфигурация
├── database.py                  # Работа с БД
├── requirements.txt             #  Зависимости
├── .env.example                 # Пример переменных окружения
├── .gitignore
│
├── handlers/                    # Обработчики команд
│   ├── start.py                 # /start, /help, /menu
│   ├── team.py                  # Управление командами
│   ├── tasks.py                 # Задачи + ConversationHandler
│   ├── callbacks.py             # Inline-кнопки
│   ├── subscription.py          # Подписки
│   ├── stats.py                 # Статистика
│   └── calendar_handler.py      # Экспорт календаря
│
├── utils/                       # Утилиты
│   ├── keyboards.py             # Inline-клавиатуры
│   ├── formatters.py            # Форматирование сообщений
│   ├── notifications.py         # Уведомления
│   ├── calendar_export.py       # Генерация .ics
│   └── validators.py            # Валидация и лимиты
│
└── scheduler/
    └── reminders.py             # APScheduler, напоминания
```

---

## 💎 Тарифные планы

| Функция | Free | Pro (₽299/мес) | Enterprise |
|---------|------|----------------|------------|
| Участники | До 3 | До 15 | Без лимита |
| Задачи | До 20 | Без лимита | Без лимита |
| Напоминания | Базовые | Все типы | Все типы |
| Календарь | ❌ | ✅ | ✅ |
| Аналитика | ❌ | ✅ | ✅ |

---

## 🛠 Технологии

- **Python 3.10+**
- **python-telegram-bot 20.7**
- **SQLite** (встроенная БД)
- **APScheduler** (планировщик)
- **icalendar** (экспорт календаря)

---

## 📌 Примеры использования

### Создание команды
```
/createteam Мой стартап
```

### Приглашение участников
```
/invite           → получить код
/join abc123def   → присоединиться
```

### Создание задачи
```
/newtask → пошаговый диалог:
1. Название задачи
2. Описание (можно пропустить)
3. Выбор исполнителя
4. Дедлайн (ДД.ММ.ГГГГ ЧЧ:ММ)
5. Приоритет (🟢/🟡/🔴)
6. Подтверждение
```

### Управление задачами
```
/mytasks   → мои задачи
/today     → на сегодня
/task 42   → детали + кнопки управления
```
